SELECT country FROM country ;
WHERE country LİKE ("A%a")

SELECT country FROM country ;
WHERE country LİKE ("_____n")

SELECT title FROM film;
WHERE title ILİKE ("%t%t%t%t%")

SELECT * FROM film ;
WHERE title LİKE ("C%") AND length > 90 AND  rental_rate =2.99
